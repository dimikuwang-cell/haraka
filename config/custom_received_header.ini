# Custom Received Header Configuration
# 自定义 Received 头部配置

# 发件服务器域名
from_domain=xzses.com

# 发件服务器主机名
from_hostname=23-94-63-137-host.colocrossing.com

# 发件服务器 IP
from_ip=23.94.63.137

# 接收服务器主机名
by_hostname=newxmmxszc38-0.qq.com

# 接收服务器信息（如 NewMX）
by_info=NewMX

# 是否使用随机 SMTP ID（true/false）
# 如果为 false，则使用下面的固定 ID
use_random_id=true

# 固定的 SMTP ID（当 use_random_id=false 时使用）
fixed_smtp_id=240F6BB
